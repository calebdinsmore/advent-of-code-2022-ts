import day0 from './index';

const INPUT = `INPUT`;

describe('On Day 5', () => {
  it(`part1 is correct`, () => {
    expect(day0.solveForPartOne(INPUT)).toBe('INPUT');
  });

  it(`part2 is correct`, () => {
    expect(day0.solveForPartTwo(INPUT)).toBe('INPUT');
  });
});
